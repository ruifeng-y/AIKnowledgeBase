/** Application layer may only depend on domain ports, not infrastructure SDKs. */
export type { AuthRepositoryPort } from '../domain/auth-repository.port';
export { AUTH_REPOSITORY } from '../domain/auth-repository.port';
