/**
 * Presentation layer placeholder.
 * Controllers arrive with business APIs in later phases.
 * Must not import Prisma / MinIO / BullMQ / @akb/db directly.
 */
export const _PRESENTATION = 'conversations-presentation' as const;
